---
title: مقدمه - انواع رویداد
sidebar_position: 1
---

# 🔥 بخش 1 — انواع رویدادها در **C#**

C# چند نوع اصلی event دارد که در Unity هم کاملاً قابل استفاده‌اند.

---

# **1) رویداد با Delegate سفارشی (Custom Delegate Event)**

### تعریف:

خودت یک delegate (نوع متد) می‌سازی و event از آن می‌سازی.

### مثال:

```csharp
public delegate void HealthChanged(int cur, int max);
public event HealthChanged OnHealthChanged;
```

### کاربرد:

وقتی می‌خواهی امضا (signature) متدهای متصل کاملاً دست خودت باشد.

---

# **2) رویداد با Action و Func (رایج‌ترین روش مدرن)**

### Action (بدون خروجی)

```csharp
public event Action OnDead;
public event Action<int,float> OnHit;
```

### Func (با خروجی)

```csharp
public Func<int> GetScoreCallback;
```

### کاربرد:

-   سریع‌تر
-   خواناتر
-   نیاز به delegate سفارشی نیست
-   در Unity بسیار رایج

---

# **3) رویداد با EventHandler (شیوه استاندارد .NET)**

### ساختار:

```csharp
public event EventHandler<MyEventArgs> OnSomething;
```

با کلاس EventArgs سفارشی:

```csharp
public class MyEventArgs : EventArgs
{
    public int Value;
}
```

### کاربرد:

در پروژه‌های بزرگ / معماری شرکتی
ولی در Unity کمتر استفاده می‌شود.

---

# 🔥 بخش 2 — انواع رویدادهای **Unity** (نه C#)

Unity خودش تعداد زیادی **event داخلی** دارد که MonoBehaviour آن‌ها را فراخوانی می‌کند.

---

# **1) Lifecycle Events (چرخه حیات)**

| رویداد          | توضیح                                        |
| --------------- | -------------------------------------------- |
| `Awake()`       | اولین بار هنگام بارگذاری اسکریپت اجرا می‌شود |
| `OnEnable()`    | وقتی فعال شود (SetActive یا Enable)          |
| `Start()`       | اولین فریم پس از فعال شدن                    |
| `Update()`      | هر فریم                                      |
| `FixedUpdate()` | هر فریم فیزیک                                |
| `LateUpdate()`  | بعد از Update                                |

---

# **2) Collision Events (برخورد)**

| رویداد             | توضیح          |
| ------------------ | -------------- |
| `OnCollisionEnter` | اولین برخورد   |
| `OnCollisionStay`  | در حال برخورد  |
| `OnCollisionExit`  | پایان برخورد   |
| `OnTriggerEnter`   | ورود به تریگر  |
| `OnTriggerStay`    | ماندن در تریگر |
| `OnTriggerExit`    | خروج از تریگر  |

برای 2D نسخه‌های 2D وجود دارد:

-   `OnCollisionEnter2D`
-   `OnTriggerEnter2D`
    و …

---

# **3) Input Events**

| رویداد           | توضیح                   |
| ---------------- | ----------------------- |
| `OnMouseDown()`  | وقتی روی آبجکت کلیک شود |
| `OnMouseUp()`    | رها شدن                 |
| `OnMouseEnter()` | ورود موس                |
| `OnMouseExit()`  | خروج موس                |

در Input System جدید → از Actionها استفاده می‌شود.

---

# **4) UI Events (EventSystem)**

از namespace `UnityEngine.EventSystems`:

| Interface              | Event            |
| ---------------------- | ---------------- |
| `IPointerClickHandler` | `OnPointerClick` |
| `IPointerEnterHandler` | `OnPointerEnter` |
| `IPointerExitHandler`  | `OnPointerExit`  |
| `IPointerDownHandler`  | `OnPointerDown`  |
| `IPointerUpHandler`    | `OnPointerUp`    |
| `IDragHandler`         | `OnDrag`         |
| `IDropHandler`         | `OnDrop`         |
| `IScrollHandler`       | `OnScroll`       |

برای UI کاملاً استاندارد.

---

# **5) Animation Events**

در Animation Clip می‌توانی یک Event ثبت کنی که یک متد را صدا بزند:

```csharp
public void OnAttackAnimationEnd() { }
```

---

# **6) Coroutines (به‌صورت رویداد غیرمستقیم)**

کال‌بک‌های مبتنی بر زمان:

```csharp
IEnumerator FadeOut()
{
    yield return new WaitForSeconds(1);
    OnFadeFinished?.Invoke();
}
```

---

# **7) UnityEvent (نسخه Inspector-Friendly)**

یو‌انیتی یک نوع event داره که از طریق Inspector هم می‌توانی listener اضافه کنی:

```csharp
public UnityEvent onDie;
public UnityEvent<int> onScoreChanged;
```

### کاربرد:

-   UI
-   رویدادهایی که می‌خواهی بدون کدنویسی متصل شوند

---

# 🔥 بخش 3 — انواع Callbackهای غیرمستقیم در Unity

| Callback                   | توضیح              |
| -------------------------- | ------------------ |
| `OnDestroy()`              | هنگام حذف شدن      |
| `OnDisable()`              | هنگام غیر فعال شدن |
| `OnApplicationQuit()`      | خروج از بازی       |
| `OnApplicationFocus(bool)` | فوکوس اپ           |
| `OnApplicationPause(bool)` | pause شدن          |

---

# 🎯 جمع‌بندی نهایی

## 🔵 **C# Event Types**

1. Delegate Events
2. Action / Func Events ← **بهترین و رایج‌ترین**
3. EventHandler / EventArgs
4. Multicast delegates

## 🔵 **Unity Event Types**

1. MonoBehaviour lifecycle
2. Collision Events
3. Input Events
4. UI Events (EventSystem)
5. Animation Events
6. Coroutine callbacks
7. UnityEvent (در Inspector)


## مقایسه


| رتبه  | نوع رویداد                                         | نیاز به تعریف Delegate؟ | سرعت اجرا         | مناسب برای معماری بازی؟ | قابل استفاده در Inspector؟ | استفاده معمول در Unity        | توضیح کوتاه                                   |
| ----- | -------------------------------------------------- | ----------------------- | ----------------- | ----------------------- | -------------------------- | ----------------------------- | --------------------------------------------- |
| **1** | **Action / Action \<T\>  Events**                      | ❌                       | ⭐⭐⭐⭐⭐ (خیلی سریع) | ⭐⭐⭐⭐⭐ (عالی)            | ❌                          | سیستم سلامت، UI، حمله، امتیاز | بهترین انتخاب مدرن — سبک، سریع، خوانا         |
| **2** | **UnityEvent / UnityEvent \<T\>**                     | ❌                       | ⭐⭐⭐⭐              | ⭐⭐⭐⭐                    | ✔ (بزرگ‌ترین مزیت)         | UI، تنظیمات در Inspector      | برای اتصال رویداد در Inspector عالیه          |
| **3** | **Custom Delegate Event**                          | ✔                       | ⭐⭐⭐⭐              | ⭐⭐⭐⭐                    | ❌                          | سیستم‌های پیچیده              | انعطاف‌پذیر ولی جا افتاده‌تر از Action        |
| **4** | **MonoBehaviour Built-in Events** (Update, Awake…) | ❌                       | ⭐⭐⭐⭐              | ⭐⭐⭐                     | ❌                          | چرخه عمر آبجکت‌ها             | توسط یونیتی صدا زده می‌شوند، برای تعاملات مهم |
| **5** | **Collision/Trigger Events**                       | ❌                       | ⭐⭐⭐               | ⭐⭐⭐                     | ❌                          | فیزیک دوبعدی/سه‌بعدی          | مخصوص برخورد — جایگزین eventهای C# نیست       |
| **6** | **EventHandler + EventArgs**                       | ✔                       | ⭐⭐⭐               | ⭐⭐                      | ❌                          | پروژه‌های شرکتی، غیر یونیتی   | در Unity زیاد کاربرد ندارد                    |
| **7** | **Animation Event**                                | ❌                       | ⭐⭐⭐               | ⭐⭐                      | ✔ (در Animator)            | زدن شمشیر، ضربه آخر           | فقط برای سینک انیمیشن — محدود                 |
| **8** | **Coroutines (به‌عنوان Callback)**                 | ❌                       | ⭐⭐                | ⭐⭐                      | ❌                          | تأخیر، زمان‌بندی              | Event واقعی نیست، ولی کاربرد زیاد دارد        |
| **9** | **UI EventSystem Interfaces** (OnPointerClick…)    | ❌                       | ⭐⭐⭐               | ⭐⭐⭐                     | ❌                          | دکمه، Drag/Drop               | فقط مخصوص UI — نه Event عمومی                 |


# 🎯 سه نوع رویداد **پرکاربرد و اصلی** در Unity/C# این‌ها هستند:

# **1) Action / Action\<T>\ (مهم‌ترین و بهترین برای گیم‌پلی)**

✔ سریع‌ترین
✔ خواناترین
✔ استاندارد C#
✔ بهترین برای سیستم‌هایی مثل: Health، Damage، Score، Inventory
✔ معماری تمیز و حرفه‌ای

مثال:

```csharp
public event Action<int> OnDamage;
```

---

# **2) Delegate سفارشی (Custom Delegate)**

✔ وقتی بخواهی امضای خاص داشته باشی
✔ وقتی readability برایت مهم باشد
✔ وقتی تعداد پارامترها زیاد است و می‌خواهی معنی‌دار باشند

مثال:

```csharp
public delegate void HealthChanged(int current, int max);
public event HealthChanged OnHealthChanged;
```

---

# **3) UnityAction / UnityEvent (برای Inspector و UI)**

✔ تنها رویدادی که می‌توانی از طریق **Inspector** به آن Listener بدهی
✔ مناسب UI، انیمیشن، Settings
✔ برای طراح بازی / Level Designerها عالیه
✖ سرعتش کمی کمتر از Action هست
✖ برای گیم‌پلی سنگین پیشنهاد نمی‌شود

مثال:

```csharp
public UnityEvent onClicked;
```

---

# 🔥 پس استفاده واقعی در Unity:

| بخش                                 | بهترین انتخاب                |
| ----------------------------------- | ---------------------------- |
| گیم‌پلی (Damage, Health, Inventory) | **Action**                   |
| معماری پیچیده با معنای خاص          | **Delegate سفارشی**          |
| UI / دکمه / Inspector               | **UnityEvent / UnityAction** |

---

# ✔ نتیجه‌گیری نهایی

**بله، در مجموع ۳ نوع رویداد اصلی و پرکاربرد داریم:**

### 🔹 Action

### 🔹 Delegate

### 🔹 UnityAction / UnityEvent

و این سه مورد حدود **۹۰٪** تمام سیستم‌های رویداد در Unity را پوشش می‌دهند.

---
